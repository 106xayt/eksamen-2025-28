# .github/workflows/sam-deploy.yml
name: SAM Validate & Deploy

on:
  pull_request:
    branches: [ main ]
    paths:
      - 'sam-comprehend/**'
      - '.github/workflows/sam-deploy.yml'
  push:
    branches: [ main ]
    paths:
      - 'sam-comprehend/**'
      - '.github/workflows/sam-deploy.yml'

env:
  AWS_REGION: eu-west-1
  CANDIDATE_NR: "28"   # <-- bytt ved behov

concurrency:
  group: sam-${{ github.ref }}
  cancel-in-progress: false

jobs:
  pr-validate:
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      # Hopp over reinstall av SAM hvis den allerede finnes på runner
      - name: Install SAM CLI (idempotent)
        run: |
          set -euo pipefail
          if command -v sam >/dev/null 2>&1; then
            echo "SAM already installed, skipping fresh install."
            sam --version
          else
            sudo apt-get update
            sudo apt-get install -y unzip curl
            curl -L -o samcli.zip https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux-x86_64.zip || \
            curl -L -o samcli.zip https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux_x86_64.zip
            unzip -q samcli.zip -d sam-installation
            sudo ./sam-installation/install --update
            sam --version
          fi

      - name: Validate
        working-directory: sam-comprehend
        run: |
          set -euo pipefail
          sam validate --template template.yaml

      - name: Build
        working-directory: sam-comprehend
        env:
          PIP_CACHE_DIR: ~/.cache/pip
        run: |
          set -euo pipefail
          sam build --use-container=false

  deploy:
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install SAM CLI (idempotent)
        run: |
          set -euo pipefail
          if command -v sam >/div/null 2>&1; then
            echo "SAM already installed, skipping fresh install."
            sam --version
          else
            sudo apt-get update
            sudo apt-get install -y unzip curl
            curl -L -o samcli.zip https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux-x86_64.zip || \
            curl -L -o samcli.zip https://github.com/aws/aws-sam-cli/releases/latest/download/aws-sam-cli-linux_x86_64.zip
            unzip -q samcli.zip -d sam-installation
            sudo ./sam-installation/install --update
            sam --version
          fi

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Validate
        working-directory: sam-comprehend
        run: |
          set -euo pipefail
          sam validate --template template.yaml

      - name: Build
        working-directory: sam-comprehend
        env:
          PIP_CACHE_DIR: ~/.cache/pip
        run: |
          set -euo pipefail
          sam build --use-container=false

      - name: Deploy
        working-directory: sam-comprehend
        run: |
          set -euo pipefail
          STACK_NAME="aialpha-sentiment-kandidat-${CANDIDATE_NR}"
          S3_BUCKET_NAME="kandidat-${CANDIDATE_NR}-data"

          sam deploy \
            --template-file .aws-sam/build/template.yaml \
            --stack-name "$STACK_NAME" \
            --capabilities CAPABILITY_IAM \
            --resolve-s3 \
            --no-confirm-changeset \
            --no-fail-on-empty-changeset \
            --parameter-overrides S3BucketName="$S3_BUCKET_NAME"
